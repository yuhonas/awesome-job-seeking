name: lint and test

on: push

jobs:
  lint-readme:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v7
    - uses: actions/setup-node@v6
    - name: Lint and Link Check
      run: make